const { bucket_name } = require("./bucket_name");
const { credentials } = require("./credentials");
const { image_base_url } = require("./image_base_url");

module.exports = { bucket_name, credentials, image_base_url };
